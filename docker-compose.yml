services:
  davinci-widget:
    build: .
    ports:
      - "3000:3000"
      - "9229:9229"
    env_file:
      - .env
    restart: unless-stopped
    command: ["node", "--inspect=0.0.0.0:9229", "server.js"]
